pywin32; sys_platform=='win32'
pythonnet; sys_platform=='win32'

click
pandas<3 # test: tolerate_version
numpy<2  # test: tolerate_version
torch
tqdm
numba
psutil
# transformers>=4.48 refactored BertEncoder so it no longer returns per-layer
# attentions (collection moved to a decorator on the top-level BertModel, which
# peptdeep bypasses by using BertEncoder directly), breaking output_attentions
# in building_block.Hidden_HFace_Transformer.
transformers<4.48 # test: tolerate_version
scikit-learn
lxml
pyteomics

alphabase>=1.5.0 # test: tolerate_version
alpharaw>=0.2.0 # test: tolerate_version
